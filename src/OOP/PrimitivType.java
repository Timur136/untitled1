package OOP;

public class PrimitivType {
    public boolean hz(boolean a, boolean b, boolean c, boolean d) {
        int i = 0;
        if (a)
            i++;
        if (b)
            i++;
        if (c)
            i++;
        if (d)
            i++;
        return i == 2;
    }
}
